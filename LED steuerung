import board
import time
import neopixel
pixels = neopixel.NeoPixel(board.D18, 30)

for i in range(0,10):
    pixels[i] = (0,255, 0)
    time.sleep(1)

for i in range(0,10):
    pixels[i] = (0,0, 0)
    time.sleep(1)
