import board
import time
import neopixel
pixels = neopixel.NeoPixel(board.D18, 156)

for i in range(0,155):
    pixels[i] = (0,255, 0)
    time.sleep(0.2)

for i in range(0,155):
    pixels[i] = (0,0,0)
